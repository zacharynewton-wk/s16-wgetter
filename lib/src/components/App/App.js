const sidebar = require('./lib/src/js/sidebar');
const main = require('./lib/src/js/main');

let sidebarMenuButton = document.getElementById('sidebar-opener');
let sidebarMenuOpen = false;
sidebarMenuButton.addEventListener('click', function(e) {
    console.log(this);
    if (sidebarMenuOpen) { // close sidebar
        
    } else { // open sidebar

    }
});